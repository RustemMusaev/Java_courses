package ru.rustem;

import ru.rustem.DAO.TicketDAO;
import ru.rustem.model.Ticket;

import java.util.List;


public class Application {
  /*  public static void main(String[] args) {

        TicketDAO ticketDAO = new TicketDAO(MyBatisConnectionFactory.getSqlSessionFactory());

        Ticket ticket = new Ticket();
        ticket.setEmail("rustem.esm@gmail.com");
        ticket.setPhone("+79130001151");
        ticket.setStatus(true);
        ticketDAO.insert(ticket);

        ticket.setEmail("ksenia@gmail.com");
        ticket.setPhone("+79134532300");
        ticket.setStatus(false);
        int id = ticketDAO.insert(ticket);

        ticketDAO.selectById(id);

        List<Ticket> tickets = ticketDAO.selectAll();

    }*/
}
